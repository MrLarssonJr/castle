module DatabaseSecret extends "@easy-k8s/Component.pkl"
import "@easy-k8s/Secret.pkl"

name = "database"
password: String
fixed key = "password"

part = new Secret {
  name = module.name
  data {
    [key] = password
  }
}
