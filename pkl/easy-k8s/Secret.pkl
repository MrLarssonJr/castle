open module Secret extends "Component.pkl"
import "@k8s/api/core/v1/Secret.pkl"

data: Mapping<String, String>

fixed resources {
  new Secret {
    metadata {
      name = module.name
    }
    stringData = module.data
  }
}
