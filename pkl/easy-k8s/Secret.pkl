module Secret extends "Part.pkl"
import "@k8s/api/core/v1/Secret.pkl"

name: String
data: Mapping<String, String>

fixed resources {
  new Secret {
    metadata {
      name = module.name
    }
    stringData = module.data
  }
}
